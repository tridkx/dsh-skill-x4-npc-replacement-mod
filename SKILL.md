---
name: x4-npc-replacement-mod
description: '给《X4：基石》(X4: Foundations) 做 **NPC 外观替换 mod**：把别的游戏/来源的角色模型（RE Engine、MMD/PMX、UE、Mixamo 骨架等）换到 X4 的 NPC 上。核心是「**换网格、留骨架**」——X4 的动画/注视/挂点全挂在共享 component 上，macro 只挑 head/torso/props 三个网格槽位，所以替换物必须带上**逐字节相同的 91 骨骼 Biped 骨架**。覆盖：**目标种族决定替换策略**（改外观池 vs 逐个 macro 覆盖 `<models>`，以及"哪些 macro 算该种族"的有效 race 判据）、源骨架 → X4 骨架的**逐骨绑定姿态转移**（单一刚体变换为什么必然失败）、**坐标手性**与三角形绕序、**UV 的 v 轴三层约定**（PMX 向下 / Blender 向上 / 导出器再翻一次）、骨轴推导（只能用主链第一个子骨）、折叠骨锚定与"一个目标只能有一个驱动者"、眼球/脚底/手指/头颈这些"高密度或跨比例"部位的处理、关节撕裂的根因与取舍、**法线**（两阶段都要开平滑）、减面与顶点预算（超预算会让空间站频闪）、材质继承与占位色、**双面可见用 TWOSIDED 而不是复制几何**、重合层沿法线外推、贴图 alpha 渗漏与 mip 链、XAC 导出器的硬约束与陷阱（含**硬编码 shader/blendmode**）、XRCatTool 打包、以及"四肢反向弯折 / 脸消失 / 衣裤鞋透明 / 贴图整体上下颠倒 / 衣服和皮肤同位置频闪 / 遮罩边缘暗斑 / 远处发丝摩尔纹 / 关节断裂 / 虎口裂开 / 脚跟离地 / 面部有面片 / 空间站频闪"这些实机症状的定位决策树。'
whenToUse: '用户要把某个角色模型做成《X4：基石》的 NPC（船员/飞行员/陆战队员/乘客等）外观，或排查已装的 X4 角色 mod 在游戏里的显示问题（动作时四肢朝反方向弯、脸整个消失、衣裤鞋透明能看穿到内壳、贴图整体上下颠倒或花纹错位、衣服与皮肤同位置出现闪烁黑斑、遮罩边缘一圈深色斑点、远处的头发或布料有爬动的摩尔纹、脚腕/肩臂/手肘处断裂或缝隙、虎口/指间裂开像少块皮、脚跟悬空只有前掌着地、手指上有一圈圈横纹、脸上有明显面片、空间站里频闪/地图卡死/地图彩色斑块、眼球不见或乱跑、头缩在衣领里、飘带被抬到头顶以上）。也适用于只想读懂/解析 X4 的 .xac / .cat / .dat，或想把 X4 原版 NPC 资产导出到 Blender 里改。'
user-invocable: true
---

# X4: Foundations NPC 外观替换 mod 制作 Skill

> ⚠️ **由 AI 生成，仅供参考。** 下面的数字与结论来自**三次**完整工程的实测，
> 换角色 / 换来源游戏 / 换目标种族 / 换 X4 版本**必须重新量**。
> 但"哪些地方一定会出错、怎么证伪"这套路线是通用的。
>
> **实测来源**（按时间顺序，后两个共用工具链）：
> 1. RE8《萝丝之影》成年萝丝（203 骨 UE/Mixamo）→ X4 9.00 **Argon** 女性（91 骨 Biped）。
>    工具链 X4CharacterConverter v0.8.7 + Blender 5.2 + XRCatTool 1.11 + RE-Mesh-Editor。
> 2. 《原神》艾梅莉埃（MMD/PMX，445 骨）→ 同为 **Argon** 女性。
> 3. 《原神》荧（MMD/PMX，272 骨）→ 同为女性但**换了种族：Terran**。
>    这一步暴露了前两个项目都没碰到的：**目标种族的 macro 继承结构不同**、
>    **UV 的 v 轴翻转**（前两个项目的贴图其实是上下颠倒的）、以及一批
>    预览渲染器自己骗人的案例。

## 0. 本文件怎么用（**动手前先读这一段**）

**本文件 = 索引 + 硬规则速查**，细节拆在 `references/` 下 10 个文档里。

- **按需 `read`**：确认要做哪一步 → 查下表 → 只读那一两个文档。
  **不要一次读完 10 个文档**。
- `§x.y` 是全局章节号，用下表定位。
- 文档里的路径示例写成 `tools/xxx.py`，指你自己工程里的脚本位置。

| 文档（`references/` 下） | 覆盖章节 | 什么时候读 |
|---|---|---|
| `00-scope-and-pipeline.md` | §0、§1 | 开工前：可行性前提、三层架构、**目标种族与替换策略**、必要工具、端到端流程、哪些结论不能跨来源搬 |
| `01-coordinate-frames.md` | §2、§3 | **最容易错、代价最大的一节**：三个坐标系、手性/绕序、**UV 的 v 轴**、判据怎么取 |
| `02-retargeting.md` | §4 | 逐骨绑定姿态转移、骨轴推导、折叠骨锚定、眼/脚/手/头颈的逐项处理、**关节撕裂的根因与取舍** |
| `03-decimation-and-normals.md` | §5、§6 | 顶点预算与频闪、减面比例、**法线（面片感的真凶）**、切线 |
| `04-materials-and-textures.md` | §7、§8 | 材质槽命名、占位色、**TWOSIDED/重合层外推/alpha 渗漏/mip 链**、BC 编码与 DDS 约束 |
| `05-export-and-packaging.md` | §9 | 导出器的硬约束与陷阱（含**硬编码 shader/blendmode**）、**macro 逐个覆盖**、打包、三个 XML |
| `06-verification.md` | §10 | **发版前必跑的校验清单**、判据工具、指标口径、**验证纪律** |
| `07-symptom-triage.md` | §11 | 实机报症状时先读这个：症状 → 根因 → 修法 |
| `08-diagnostic-discipline.md` | §12 | **诊断脚本自己出错时**：尺子先被证伪、与管线共享配置、预览≠实机 |
| `09-legs-and-lateral-damping.md` | §13 | **腿与步态**：把"猫步"翻译成可测的量、**横向阻尼不能沿骨链逐级递减**、动画不动类问题的排错清单、量成品资产、**发版后先核对游戏里装的是哪一版** |

## 1. 七条硬规则（违反必然出问题）

### 1.1 骨架必须与 vanilla **逐字节相同**

X4 的 NPC 是"换网格、留骨架"：`libraries/character_components.xml` 里
`character_argon_female_01` 这类共享 component 拥有骨骼与 1100+ 条动画，
macro 只挑 head/torso/props 三个**网格**。所以替换物**不能改骨架**——
不是"尽量一致"，是逐字节。用 `tools/xac.py` 的 `compare_skeletons()` 比：
**骨骼名顺序一致 + bind payload 91/91 逐字节相同**才算过。

> 原版躯干资产里整套 91 骨骼**重复 4 次**（364 = 4×91，源 .max 合并所致）。
> 按名字去重的比较器会静默吞掉副本并给出错误结论，**比对必须保序**。

**这条规则也决定了关节撕裂无法根治**：源骨架与 X4 骨架的脊柱/腿长比例不同时，
撕裂只能缓解不能消除，因为改比例就等于改骨架。取舍见 §4.9。

### 1.2 坐标映射可能是**反射**（det = −1），那就必须反转三角形绕序

**不同来源游戏的空间约定不同，先量再写**：

```
RE8 源空间    (x, up, forward)  米     映射 (x, y, z) → (x, z, y) × 100     det = −1 → 必须反转绕序
MMD/PMX      (x, up, back)      单位   映射 (x, y, z) → (x, −z, y)          det = +1 → 保持绕序
```

符号**由解剖学定，不由拟合定**。MMD 的判据（三个独立证据）：

```
左目   − 頭      = (0, +0.68, −0.72)   眼睛在 −Z
齿下   − 頭      = (0,  0.00, −1.08)   牙在 −Z
左足先EX − 左足首D = (0, −0.43, −0.83)   脚尖在 −Z
→ −Z 是「前」；X4 的 left_eye_dummy / Toe0 都在 +Y → z 映射到 +y
```

交换两个轴 = **det = −1 的反射**，于是：

1. **量出的顶点位置是对的，但每个三角形都朝内** → 引擎剔除正面 → 衣服透明、
   能看穿到对面内壳、光照错乱像揉皱的破布、脸整片消失。
   **修法：建网格时把每个三角形反过来** `(a,b,c) → (c,b,a)`。
2. 如果反过来写成 `(x, −z, y)`（det = +1），顶点位置**前后镜像**，
   而镜像是一种反射、**只用旋转的 Kabsch 永远修不回来** —— 残差会全堆在
   前后方向偏离最大的部位（手 42 cm、脚趾 23 cm），而近似对称的躯干只有几厘米。
   **这个分布极具迷惑性，会被误读成"姿态差异"。**

**判据**：`tools/check_winding.py` 直接比较 **PMX 自带的逐顶点法线**与几何面法线
（源文件自己会说话，不用推理）：
`mean dot(face normal, vertex normal) > 0 → 源绕序与右手法则一致`。
附带 `check_normals.py` 逐网格统计"面法线背离质心"的比例：healthy 0.55–0.85，
翻转的网格恰好落在 0.5 以下。**这个错误骗得过所有基于顶点位置的检查。**

### 1.3 两段式管线里，**两段都要设平滑着色**

导出器取的是 `loop.normal`。若 stage2 用 `from_pydata` 重建网格而没设
`use_smooth`，Blender 默认平面着色，**每个三角形带着自己的面法线进 .xac**，
实机就是满脸面片。

```
导入成品 xac 后自检：|顶点法线 − 面法线|   0.000 = 平面着色（错）  0.4 左右 = 平滑（对）
```

**注意**：这个判据要跑在**导出的 .xac** 上，不是跑在 Blender 场景上。
实测对照（荧，mean |vn − fn|）：head 0.186 / body 0.242，**vanilla 0.205**。

**顺带的大收益**：平滑法线会消掉**法线接缝**，而 X4 的导出计数是
"每条 UV 接缝和法线接缝各算一个顶点" —— 实测导出顶点从 45 189 掉到 13 355
（−70%）。**法线值钱程度和减面相当。**

### 1.4 目标种族决定替换策略 —— **不能假设 macro 的继承方式**

**先查目标种族的 macro 是怎么组装模型的，再决定改哪一层。**
同一个游戏里不同种族可能完全不同：

```
Argon 女性：  派生 macro 继承 base 的 <models>        → 改外观池（charactergroups）就够
Terran 女性： 派生 macro 每个都重写自己的 <models>   → 改 base 无效，必须逐个 macro 替换
```

**判据**：把每个候选 macro 的 `<models>` 块打出来，看有多少个非空。全都有 → 逐个替换。

**逐个替换比改池更好**，理由有两条：
- 池仍然挑选不同的 macro，**NPC 的身份（名字、背景、职称、语音）保持多样**；
- **剧情/任务 NPC 一并覆盖** —— 它们由脚本直接 `ref`，从不经过外观池。

**"哪些 macro 算目标种族"必须按数据判，不能按名字判。** 判据是沿 `ref` 链解析出的
**有效 `race` + `female` + `faction`**。名字极具误导性，两个方向都会错：

| macro | 名字看起来 | 实际 |
|---|---|---|
| `character_yaki_female_cau_base_01_macro` | Yaki 女性 | `race="argon"` ✗ |
| `character_yaki_female_plot_yaki_civilian_macro` | Yaki 女性 | `race="terran"` ✓ |
| `character_player_custom_f_terran_cau_macro` | Terran 女性 | `faction="player"` **是玩家自己** ✗ |
| `character_scenario_combat_ter_gunner_macro` | Terran 剧情 | torso 是 `char_ter_**m**_pilot_suit_01`，男性 ✗ |
| `character_scenario_combat_ter_marine_macro` | 名字里没有性别 | 靠 `ref` 才是女性 ✓ |

还有一条兜底：有的 macro `ref` 的是**别的种族**的 helper，有效 race 因此是 argon，
但**目标势力的池选的就是它们**（Terran/Pioneer 外交官）。判据是
**"是否被 `terran.*` / `pioneers.*` 的 female 池选中"**，**不是**"是否穿该种族的制服" ——
后者会把一个 Yaki 外交官（恰好发了同一套制服）也拉进来。

> **池的检查必须递归展开 `<select character=...>` 间接链接**（池可以指向池）。
> 这个检查在实际工程里抓出过 2 个漏网的外交官 macro。

### 1.5 UV 的 v 轴：**三层约定，两次翻转，必须算清**

与 §1.2 的顶点坐标手性**并列的第二个"帧"问题**，同样只在实机暴露。

```
PMX 的 v：0 在图像顶端（向下）
Blender 的 v：0 在图像底端（向上）        -> 差一次 1-v
导出器 addon.py:855   v_x4 = 1 - v_blender
导入器 addon.py:531   v_blender = 1 - v_x4   （对称，所以 v_x4 与 Blender 反号）
```

**推导**：X4 的 v 与 PMX 一样向下，所以目标是 X4 里的 v 等于 `v_pmx`。
导出器会翻一次 ⇒ **Blender 里必须存 `1 - v_pmx`**。
stage 若原样传递 `v_pmx`，X4 拿到的就是 `1 - v_pmx` —— **整张贴图上下颠倒**。

**可复现的验证链**（导入成品 xac 后读 v 范围，与源对比）：

```
源 PMX skin      v [0.0615, 0.9263]
期望 stage 存 1-v [0.0737, 0.9385]
导入 xac 后       [0.0737, 0.9385]   ✓
```

> **为什么只有实机才发现**：软件渲染预览常用**源 PMX 的 UV**（不经 Blender），
> 所以预览一直是对的。**预览与实机走的不是同一条路时，预览的"正常"不算数。**

### 1.6 背面可见用 `blendmode="TWOSIDED"`，**不要复制几何**

MMD 模型大量使用"单面几何 + 材质标记双面"。X4 默认剔除背面，所以薄片（发片、
衣、裙、飘带、睫毛）需要显式双面。**别用复制反向几何的办法**：

| 做法 | 结果 |
|---|---|
| 复制反向面，**共享顶点** | 顶点法线被正反两面平均到零 → 渲染成惨白碎条；导出的 `loop.normal` 同样是坏的 |
| 复制反向面，**独立顶点** | 位置完全相同 = **同一深度 → 实机频闪**；且顶点数翻倍 |
| **`blendmode="TWOSIDED"`** | ✓ 零几何成本，不可能和自己打架 |

vanilla 有先例：`p1_char_spl_f_cloak_gen_01` 用的正是 `p1_character` + `TWOSIDED`。

实测收益（荧）：head 8195 → **4813**（vanilla 4693 的 1.03×），
body 20513 → **11325**（vanilla 3601 的 3.15×），mod 3.0 → 2.2 MB。
**这一个决定同时解决了频闪和顶点预算两个问题。**

### 1.7 横向阻尼**不能沿骨链逐级递减** —— 那是"猫步"的根因

源腿内收、X4 外撇时，逐骨对齐会把腿甩出裙子，所以只保留一部分横向位移。
但折扣**不能一节比一节小**（实测，荧）：

```
大腿 0.60 / 小腿 0.30 / 脚 0.20  → 骨链 11.61 → 10.62 → 9.75 → 8.21   折向内
                                    两脚间距 16.4 cm（几何 18.0）   vanilla 39.8
大腿 0.60 / 小腿 0.85 / 脚 0.95  → 骨链 10.62 → 13.72 → 17.11         单调外撇
                                    两脚间距 35.9 cm               脚踝偏差 0.28 cm
```

逐级递减不只是把腿**平移**，它把骨链**折弯**：关节落在它所驱动的几何内侧，
绑定姿态看不出来，动画一播就变成"两只脚在一条线上走"。

两条判据：**骨链横向单调外撇**（大腿 < 小腿 < 脚踝）+ **两脚间距 ≈ vanilla**。
注意"只推到 vanilla 的 77% 仍然看得出猫步"——**跟 vanilla 比，不要跟上一版比**。
排错清单与工具见 `09-legs-and-lateral-damping.md`（§13）。

## 2. 数字速查（实测，仅供参考）

| 项 | 萝丝（RE8） | 荧（MMD） |
|---|---|---|
| 骨骼数 | X4 侧 91；源侧 203 | X4 侧 91；源侧 272 |
| 全局拟合 scale | — | 9.7746（艾梅莉埃 9.625，**同来源不同作者也不同**）|
| 顶点预算 | vanilla ~5k/资产；**6× 安全**，15× 会让空间站频闪 | head 1.03× / body 3.15× |
| 减面比例 | hair 0.10 / jacket 0.35 / body 0.30 / face 0.50 / hands **1.0** | **不减**（图集 UV 一减就毁）|
| 全局拟合残差 | Head 2.8 / Hip 9.8 / 手 25 / 脚趾 10.6 cm | 脚踝 13.5 / 髋 8.4 / 手肘 7.7 cm |
| 映射结果 | — | 210 带权骨 → 53 直接 + 157 折叠 + **0 未映射** |
| 脚底 | 对齐到 −0.5 cm（vanilla 鞋底 −0.32）| 同 |

## 3. 最容易踩的顺序性陷阱

1. **坐标系符号**（§1.2）—— 先证伪再往下走。判据不是"拟合残差小"，而是
   **解剖学方向**（眼球/脚趾朝前）+ **绕序检查**。
2. **UV 的 v 轴**（§1.5）—— 与上一条并列，同样只在实机暴露。
3. **先做逐骨转移，再谈姿态对齐**。用"绕肩旋转手臂"这类**局部 hack**去救
   全局问题，会把项目带进死胡同。真正的错因是"一次刚体变换无法跨越两套骨架的
   姿态+比例差异"。
4. **一个 X4 骨只能有一个直接驱动者**。两个源骨写同一个目标时，**谁赢取决于
   字典遍历顺序**。实测：把 `上半身3` 和 `上半身2` 都映射到 `Spine2`，位置更低的
   `上半身3` 赢了，`Spine2` 的源位置从 ~132 变成 119，所有绑在它上面的顶点被
   **向上平移 13.9 cm** —— 两条飘带（整条 19 节骨链折叠到 Spine2）**从肩部被抬到
   头顶以上**。修法：让低优先级的那个折叠到它的父骨。**加断言，让重复目标直接报错。**
5. **骨轴推导里任何"取平均"都可能在分叉骨上给出与解剖学无关的方向**。
6. **权重改动必须在 `transform()` 之前**（改源权重），不是在之后改成品权重 ——
   顶点位置是在 `transform()` 内部用权重算出来的，事后改权重对几何**零影响**。
   实测：事后 rebind 移动了 674 个顶点，折角变化 **0.00 cm**。见 §4.9。
7. **诊断脚本本身要先被证伪**（见 `08-diagnostic-discipline.md`）。
8. **改一处、跑一次、看一次**。改完不重跑管线（或导出器静默失败）会让人
   对着旧产物调试 —— 见 §10 的"验证纪律"。

## 4. 快速索引：症状 → 去哪一节

| 实机症状 | 先查 |
|---|---|
| 动作时四肢朝反方向弯、手指拉长 | `07-symptom-triage.md` §11.1（姿态/比例，不是坐标轴） |
| 脸整个消失 / 衣裤鞋透明、看到内壳 | `01-coordinate-frames.md` §3（绕序） |
| **贴图整体上下颠倒 / 花纹错位** | `01-coordinate-frames.md` §3.6（**UV 的 v 轴**）|
| **衣服与皮肤同位置出现闪烁黑斑** | `04-materials-and-textures.md`（重合层外推）|
| **遮罩边缘一圈深色斑点** | `04-materials-and-textures.md`（alpha 渗漏）|
| **远处头发/布料有爬动的摩尔纹** | `04-materials-and-textures.md`（mip 链）|
| **关节处断裂/台阶/缝隙** | `02-retargeting.md` §4.10（**已知问题**，含取舍）|
| **飘带/链条被抬到不该在的高度** | §3 第 4 条（一个目标两个驱动者）|
| 腰部有莫名其妙的横向色带、衣服颜色不对 | `04-materials-and-textures.md` §7（无 albedo 材质的占位色） |
| 虎口/指间裂开像少一块皮 | `02-retargeting.md` §4.5（手指绑手掌） |
| 脚跟悬空、只有前掌着地 | `02-retargeting.md` §4.6（脚部共用位移 + 地面双向对齐） |
| 手指上有一圈圈横纹 | `03-decimation-and-normals.md` §6（切线跟着 UV，手部别减面） |
| 脸上有明显面片 | `03-decimation-and-normals.md` §5（两阶段都要 use_smooth） |
| 空间站频闪 / 地图卡死 / 地图彩色斑块 | `03-decimation-and-normals.md` §4（顶点预算） |
| 眼球不见、或偶尔乱跑不跟头 | `02-retargeting.md` §4.4（真眼球在 face 里 + look-at 会甩飞） |
| 头缩在衣领里 | `02-retargeting.md` §4.8（脖子长度差异 → 头颈共用位移） |
| 改完没效果 / 产物没变 | `05-export-and-packaging.md` §9.3（导出器陷阱） |
| **走路像猫步 / 两脚落在一条线上 / 坐姿两脚贴得近** | `09-legs-and-lateral-damping.md` §13（**横向阻尼逐级递减**；判据是两脚间距，跟 vanilla 比） |
| **静止姿态正常，只有动画里不对** | `09-legs-and-lateral-damping.md` §13.3（骨架/镜像/权重/源数据逐项证同的清单） |
| **修了但"没用"、改了半天没变化** | `09-legs-and-lateral-damping.md` §13.5：**先核对游戏 `extensions/<id>/content.xml` 的 version** —— 很可能装的是旧版，压根没生效 |
| **材质设了 ALPHA1/TWOSIDED 但游戏里没生效** | `05-export-and-packaging.md`（导出器硬编码） |
| **诊断结论与实机不符 / 预览说没问题** | `08-diagnostic-discipline.md` |
