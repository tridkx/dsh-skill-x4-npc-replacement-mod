---
name: x4-npc-replacement-mod
description: 给《X4：基石》(X4: Foundations) 做 **NPC 外观替换 mod**：把别的游戏/来源的角色模型（RE Engine、UE、Mixamo 骨架等）换到 X4 的 NPC 上。核心是「**换网格、留骨架**」——X4 的动画/注视/挂点全挂在共享 component 上，macro 只挑 head/torso/props 三个网格槽位，所以替换物必须带上**逐字节相同的 91 骨骼 Biped 骨架**。覆盖：源骨架 → X4 骨架的**逐骨绑定姿态转移**（单一刚体变换为什么必然失败）、**坐标手性**与三角形绕序（前后轴符号错了会镜像，而镜像修不回来）、骨轴推导（只能用主链第一个子骨）、折叠骨锚定、眼球/脚底/手指/头颈这些"高密度或跨比例"部位的处理、**法线**（两阶段都要开平滑，否则实机满脸面片）、减面与顶点预算（超预算会让空间站频闪）、材质继承与占位色、XAC 导出器的硬约束与陷阱、XRCatTool 打包、以及"四肢反向弯折 / 脸消失 / 鞋裤透明 / 虎口裂开 / 脚跟离地 / 手指环纹 / 面部有面片 / 空间站频闪"这些实机症状的定位决策树。实测自 RE8《萝丝之影》成年萝丝 → Argon 女性 NPC，X4 9.00。
whenToUse: 用户要把某个角色模型做成《X4：基石》的 NPC（船员/飞行员/陆战队员/乘客等）外观，或排查已装的 X4 角色 mod 在游戏里的显示问题（动作时四肢朝反方向弯、脸整个消失、衣裤鞋透明能看穿到内壳、虎口/指间裂开像少块皮、脚跟悬空只有前掌着地、手指上有一圈圈横纹、脸上有明显面片、空间站里频闪/地图卡死/地图彩色斑块、眼球不见或乱跑、头缩在衣领里）。也适用于只想读懂/解析 X4 的 .xac / .cat / .dat，或想把 X4 原版 NPC 资产导出到 Blender 里改。
user-invocable: true
---

# X4: Foundations NPC 外观替换 mod 制作 Skill

> ⚠️ **由 AI 生成，仅供参考。** 下面的数字与结论来自**一次**完整工程（RE8 萝丝 →
> X4 Argon 女性）的实测，换角色 / 换来源游戏 / 换 X4 版本**必须重新量**。
> 但"哪些地方一定会出错、怎么证伪"这套路线是通用的。
>
> **实测来源**：RE8《萝丝之影》成年萝丝（203 骨 UE/Mixamo 风格）→ X4 9.00
> Argon 女性 NPC（91 骨 3ds Max Biped），工具链 X4CharacterConverter v0.8.7 +
> Blender 5.2 + XRCatTool 1.11 + RE-Mesh-Editor。

## 0. 本文件怎么用（**动手前先读这一段**）

**本文件 = 索引 + 硬规则速查**，细节拆在 `references/` 下 8 个文档里。

- **按需 `read`**：确认要做哪一步 → 查下表 → 只读那一两个文档。
  **不要一次读完 8 个文档**。
- `§x.y` 是全局章节号，用下表定位。
- 文档里的路径示例写成 `tools/xxx.py`，指你自己工程里的脚本位置。

| 文档（`references/` 下） | 覆盖章节 | 什么时候读 |
|---|---|---|
| `00-scope-and-pipeline.md` | §0、§1 | 开工前：可行性前提、三层架构（charactergroups → macros → .xac）、必要工具、端到端流程、哪些结论不能跨来源搬 |
| `01-coordinate-frames.md` | §2、§3 | **最容易错、代价最大的一节**：三个坐标系、手性/绕序、判据怎么取（眼球/脚趾） |
| `02-retargeting.md` | §4 | 逐骨绑定姿态转移、骨轴推导、折叠骨锚定、眼/脚/手/头颈的逐项处理 |
| `03-decimation-and-normals.md` | §5、§6 | 顶点预算与频闪、减面比例、**法线（面片感的真凶）**、切线 |
| `04-materials-and-textures.md` | §7、§8 | 材质槽命名、没有 albedo 的材质、占位色、BC 编码与 DDS 约束 |
| `05-export-and-packaging.md` | §9 | XAC 导出器的硬约束与陷阱、打包、三个 XML、测试期"全部替换"模式 |
| `06-verification.md` | §10 | **发版前必跑的校验清单**、四个判据工具、指标口径 |
| `07-symptom-triage.md` | §11 | 实机报症状时先读这个：症状 → 根因 → 修法 |

## 1. 三条硬规则（违反必然出问题）

### 1.1 骨架必须与 vanilla **逐字节相同**

X4 的 NPC 是"换网格、留骨架"：`libraries/character_components.xml` 里
`character_argon_female_01` 这类共享 component 拥有骨骼与 1100+ 条动画，
macro 只挑 head/torso/props 三个**网格**。所以替换物**不能改骨架**——
不是"尽量一致"，是逐字节。用 `tools/xac.py` 的 `compare_skeletons()` 比：
**骨骼名顺序一致 + bind payload 91/91 逐字节相同**才算过。

> 原版躯干资产里整套 91 骨骼**重复 4 次**（364 = 4×91，源 .max 合并所致）。
> 按名字去重的比较器会静默吞掉副本并给出错误结论，**比对必须保序**。

### 1.2 坐标映射是**反射**（det = −1），必须反转三角形绕序

```
RE8 源空间   (x, up, forward)      米
X4 / Blender (x, forward, up)      厘米
映射：        (x, y, z) → (x, z, y) × 100
```

符号**由解剖学定，不由拟合定**：

```
Rose 眼球几何在 Head 骨的 +z 侧（+4.9 cm）    X4 ter_eyeball 在 Bip01 Head 的 +y 侧（+9.5 cm）
Rose 脚趾在 +z                              X4 Toe0 在 +y
→ 两者的 +z / +y 都是「前」→ z 必须映射到 +y，不能取负
```

交换两个轴 = **det = −1 的反射**，于是：

1. **量出的顶点位置是对的，但每个三角形都朝内** → 引擎剔除正面 → 衣服透明、
   能看穿到对面内壳、光照错乱像揉皱的破布、脸整片消失。
   **修法：建网格时把每个三角形反过来** `(a,b,c) → (c,b,a)`。UV 不受影响
   （UV 逐顶点/逐 loop 带过去）。
2. 如果反过来写成 `(x, −z, y)`（det = +1），顶点位置**前后镜像**，
   而镜像是一种反射、**只用旋转的 Kabsch 永远修不回来** —— 残差会全堆在
   前后方向偏离最大的部位（手 42 cm、脚趾 23 cm），而近似对称的躯干只有几厘米。
   **这个分布极具迷惑性，会被误读成"姿态差异"。**

**判据**：`tools/check_normals.py`，逐网格统计"面法线背离质心"的比例。
healthy 0.55–0.85；翻转的网格恰好落在 0.5 以下（精确翻转 = 1 − 原值）。
**这个错误骗得过所有基于顶点位置的检查**，只有它和实机截图能发现。

### 1.3 两段式管线里，**两段都要设平滑着色**

导出器取的是 `loop.normal`。若 stage2 用 `from_pydata` 重建网格而没设
`use_smooth`，Blender 默认平面着色，**每个三角形带着自己的面法线进 .xac**，
实机就是满脸面片。

```
导入成品 xac 后自检：|顶点法线 − 面法线|   0.000 = 平面着色（错）  0.4 左右 = 平滑（对）
```

**顺带的大收益**：平滑法线会消掉**法线接缝**，而 X4 的导出计数是
"每条 UV 接缝和法线接缝各算一个顶点" —— 实测导出顶点从 45 189 掉到 13 355
（−70%）。**法线值钱程度和减面相当。**

## 2. 数字速查（实测，仅供参考）

| 项 | 值 |
|---|---|
| 骨骼数 | X4 侧 91（Biped）；源侧本例 203 |
| 顶点预算 | vanilla ~5k/资产；**6× 安全**，15× 会让空间站频闪 |
| 减面比例（本例终值） | hair 0.10 / jacket 0.35 / body 0.30 / face 0.50 / hands **1.0（不减）** |
| 全局拟合残差 | Head 2.8 cm / Hip 9.8 cm / 手 25 cm / 脚趾 10.6 cm（手脚本例属真实比例差异，逐骨转移解决） |
| 逐骨转移后绑定误差 | 手指 0.96 cm / 脚趾 5.8 / 手掌 5.2 / 总体 ~10（vanilla 7.2） |
| 脚底 | 对齐到 −0.5 cm（vanilla 鞋底 −0.32） |

## 3. 最容易踩的顺序性陷阱

1. **坐标系符号**（§1.2）—— 先证伪再往下走。判据不是"拟合残差小"，而是
   **解剖学方向**（眼球/脚趾朝前）+ **绕序检查**。
2. **先做逐骨转移，再谈姿态对齐**。用"绕肩旋转手臂"这类**局部 hack**去救
   全局问题，会把项目带进死胡同（本例卡了很久）。真正的错因是"一次刚体变换
   无法跨越两套骨架的姿态+比例差异"。
3. **骨轴推导里任何"取平均"都可能在分叉骨上给出与解剖学无关的方向**。
   骨盆用"子骨均值"会得到 161° 旋转，把所有下摆/裤子顶点转翻。
4. **诊断脚本本身要先被证伪**。本例中途多个诊断脚本把两个坐标系混着比、
   换算函数写反，导致被错误结论带偏好几轮。**结论要沉淀成可视化/可复现的判据**。
5. **改一处、跑一次、看一次**。改完不重跑管线（或导出器静默失败）会让人
   对着旧产物调试 —— 见 §10 的"验证纪律"。

## 4. 快速索引：症状 → 去哪一节

| 实机症状 | 先查 |
|---|---|
| 动作时四肢朝反方向弯、手指拉长 | `07-symptom-triage.md` §11.1（姿态/比例，不是坐标轴） |
| 脸整个消失 / 衣裤鞋透明、看到内壳 | `01-coordinate-frames.md` §3（绕序） |
| 腰部有莫名其妙的横向色带、衣服颜色不对 | `04-materials-and-textures.md` §7（无 albedo 材质的占位色） |
| 虎口/指间裂开像少一块皮 | `02-retargeting.md` §4.5（手指绑手掌） |
| 脚跟悬空、只有前掌着地 | `02-retargeting.md` §4.6（脚部共用位移 + 地面双向对齐） |
| 手指上有一圈圈横纹 | `03-decimation-and-normals.md` §6（切线跟着 UV，手部别减面） |
| 脸上有明显面片 | `03-decimation-and-normals.md` §5（两阶段都要 use_smooth） |
| 空间站频闪 / 地图卡死 / 地图彩色斑块 | `03-decimation-and-normals.md` §4（顶点预算） |
| 眼球不见、或偶尔乱跑不跟头 | `02-retargeting.md` §4.4（真眼球在 face 里 + look-at 会甩飞） |
| 头缩在衣领里 | `02-retargeting.md` §4.7（脖子长度差异 → 头颈共用位移） |
| 改完没效果 / 产物没变 | `05-export-and-packaging.md` §9.3（导出器陷阱） |
